{
  "name": "backend",
  "version": "1.0.0",
  "description": "CRUD com API (backend) e Client (frontend) para gerenciamento de clientes",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon src/server.js --watch src",
    "db:migrate": "npx sequelize db:migrate",
    "db:seed": "npx sequelize db:seed:all"
  },
  "keywords": [],
  "author": "Willian Cozzi Candido",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.6",
    "pg": "^8.5.1",
    "pg-hstore": "^2.3.3",
    "request": "^2.88.2",
    "sequelize": "^6.3.5",
    "yup": "^0.32.8"
  },
  "devDependencies": {
    "sequelize-cli": "^6.2.0",
    "sucrase": "^3.17.0"
  }
}
